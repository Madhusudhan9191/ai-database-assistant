services:

  backend:
    build: .
    container_name: ai-db-backend
    ports:
      - "8000:8000"
    env_file:
      - .env

  frontend:
    build:
      context: ./ai-db-frontend
    container_name: ai-db-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend